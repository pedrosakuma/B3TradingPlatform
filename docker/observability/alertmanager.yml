route:
  receiver: local-webhook
  group_by: [alertname, subsystem]
  group_wait: 1s
  group_interval: 5s
  repeat_interval: 4h

receivers:
  - name: local-webhook
    webhook_configs:
      - url: http://alert-receiver:8080/alerts
        send_resolved: true
