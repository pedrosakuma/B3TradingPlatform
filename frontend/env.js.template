// Deploy-time configuration for the static frontend. This file is a
// template: the entrypoint script (docker-entrypoint.d/20-render-env-js.sh)
// renders it into /usr/share/nginx/html/js/env.js at container start.
//
// No secrets belong here: these values are intentionally public browser
// configuration. Entra uses Authorization Code + PKCE through MSAL, so the
// SPA has a client id and delegated API scope but never a client secret.
window.__B3_CONFIG__ = {
  marketDataWsUrl: __MARKETDATA_WS_URL_JSON__,
  appTitle: __APP_TITLE_JSON__,
  auth: {
    mode: __AUTH_MODE_JSON__,
    localLoginEnabled: __AUTH_LOCAL_LOGIN_ENABLED_JSON__,
    signupEnabled: __AUTH_SIGNUP_ENABLED_JSON__,
    totpEnabled: __AUTH_TOTP_ENABLED_JSON__,
    authority: __AUTH_AUTHORITY_JSON__,
    clientId: __AUTH_CLIENT_ID_JSON__,
    apiScope: __AUTH_API_SCOPE_JSON__,
    redirectUri: __AUTH_REDIRECT_URI_JSON__,
    logoutUri: __AUTH_LOGOUT_URI_JSON__,
    knownAuthorities: __AUTH_KNOWN_AUTHORITIES_JSON__,
  },
};
